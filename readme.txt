First run:
docker-compose up -d 

If changes were made in php Dockerfile, use below command to re-build the image
docker compose build --no-cache

Accesss phpMyAdmin with localhost:8080
*Suggested to change the default password